// AP English Language · Rhetorical devices bank.
// STUB awaiting its writer, who replaces it wholesale with grouped
// ReferenceGroup[] entries: every device gets `name`, a one-sentence
// definition in `text`, an ORIGINAL example sentence in `note`
// (never a quoted copyrighted example), grouped by function
// (appeals, diction/tone, syntax, figurative language, argument moves).

import type { ReferenceGroup } from "../../courseTypes";

export const deviceGroups: ReferenceGroup[] = [];
